# Local Windows broker config for ColdChain AI development.
# (mosquitto.conf is the Docker config; this one avoids the
#  /mosquitto/data persistence path used inside containers.)

listener 1883 0.0.0.0

# Anonymous access for local development only.
allow_anonymous true

# Store the persistence DB next to this file.
persistence false

log_dest stdout