# Mosquitto MQTT broker configuration for ColdChain AI
# Listening on all interfaces so containers/hosts can connect.

listener 1883 0.0.0.0

# Allow anonymous access for local development only.
# Switch to false + credentials for production.
allow_anonymous true

persistence true
persistence_location /mosquitto/data/

log_dest stdout